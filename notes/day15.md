Isn't this just least common multiple? <- Tried this approach but couldn't figure out so went with brute force with some optimization.

I think the solution I have is fairly clean using Stream.iterate.

TODO: Research CRT (Chinese remainder theorum) which is apparently the correct way to solve this.
